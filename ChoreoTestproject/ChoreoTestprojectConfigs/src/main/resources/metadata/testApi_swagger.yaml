openapi: 3.0.1
info:
  title: testApi
  description: API Definition of testApi
  version: 1.0.0
servers:
- url: http://localhost:8290/testapi
paths:
  /pet/{petid}:
    get:
      parameters:
      - name: petid
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
