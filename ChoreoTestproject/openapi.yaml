openapi: 3.0.1
info:
  title: testApi
  description: API Definition of testApi
  version: 1.0.0
servers:
  - url: "http://localhost:8290"
paths:
  /testApi/pets:
    get:
      responses:
        default:
          description: Default response
  "/testApi/pets/{petid}":
    get:
      parameters:
        - name: petid
          in: path
          required: true
          schema:
            type: integer
            example: 1
      responses:
        default:
          description: Default response